{
  "name": "mern-bloglist",
  "version": "1.0.0",
  "description": "mern-stack bloglist app",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ~/Fullstackopen/main-repo/osa5/bloglist-frontend && npm run build --prod && cp -r build ~/Fullstackopen/mern-bloglist",
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "deploy": "git push heroku main",
    "lint": "eslint .",
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "start:test": "NODE_ENV=test node index.js"
  },
  "author": "Jaakko Hurtta",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.13",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "eslint": "^7.28.0",
    "jest": "^27.0.4",
    "lodash": "^4.17.21",
    "supertest": "^6.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
